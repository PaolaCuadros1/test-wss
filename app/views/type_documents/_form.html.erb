<%= form_with(model: type_document, local: true) do |form| %>
  <% if type_document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type_document.errors.count, "error") %> prohibited this type_document from being saved:</h2>

      <ul>
      <% type_document.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :type_document_name %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.number_field :type, id: :type_document_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
